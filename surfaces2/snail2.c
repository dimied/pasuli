
#include "snail2.h"
#include "surfaces_c_includes.h"

#if (USE_SNAIL2 != 0)
void Snail2(pasuli_vartype u,
            pasuli_vartype v,
            pasuli_consttype *constants,
            PaSuLiObject *pO);
#endif
