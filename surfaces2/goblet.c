
#include "goblet.h"
#include "surfaces_c_includes.h"

#if (USE_GOBLET != 0)
void Goblet(pasuli_vartype u,
            pasuli_vartype v,
            pasuli_consttype *constants,
            PaSuLiObject *pO);
#endif
